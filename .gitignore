node_modules/
dist/
.next/
*.tsbuildinfo
.env
.env.local
*.log
# Runtime halt / contest snapshots stay local-only.
hackathon/HALT
hackathon/CONTEST.json
# Desk baseline (ledger, last-scan, last-tape, loop-status) is committed
# so Heroku deploys show the same cockpit as local.
data/
.DS_Store
